// Stubbed for Task 1 (tldraw removed). Full implementation in Task 2.
import type { TerminalShape } from "./TerminalShape";

export function TerminalWindow(_props: { shape: TerminalShape }) {
  return null;
}
